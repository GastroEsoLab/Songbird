% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BackgroundDecay.R
\name{optimise_symmetric_x}
\alias{optimise_symmetric_x}
\title{optimise_symmetric_x}
\usage{
optimise_symmetric_x(
  bedpe_file,
  bin_data,
  min_length = 50L,
  max_length = 1000L,
  tag_overlap = 10L,
  max_dist = 200000L,
  band_width = 500L,
  n_sample = 5000L,
  plateau_tol = 0.05
)
}
\arguments{
\item{bedpe_file}{character — path to a single \code{.bedpe.gz} file.}

\item{bin_data}{data.frame — QDNAseq bin annotations (from
\code{process.batch} internals), used to assign fragments to bins.}

\item{min_length}{integer — minimum fragment length (same as in
\code{process.batch}). Default 50.}

\item{max_length}{integer — maximum fragment length. Default 1000.}

\item{tag_overlap}{integer — Tn5 footprint size in bp. Default 10.}

\item{max_dist}{integer — maximum distance from reference start to
profile. Default 200000 bp.}

\item{band_width}{integer — width of each distance band in bp. Default 500.}

\item{n_sample}{integer — number of reference reads to sample. Default
5000. More gives a smoother profile but takes longer.}

\item{plateau_tol}{numeric — fraction of far-field mean within which density
is considered flat. Default 0.05 (5%).}
}
\value{
integer — suggested \code{symmetric_x} in bp.
}
\description{
Finds the optimal \code{symmetric_x} background window size by profiling
the background read density as a function of distance from reference read
starts (via \code{\link{plot_background_decay}}) and returning the distance
at which density has decayed to within \code{plateau_tol} of the far-field
mean.
}
\details{
This is the same logic as \code{\link{plot_background_decay}} but returns
the numeric value directly for use in \code{\link{process.batch}}.
}
\examples{
\dontrun{
opt_x <- optimise_symmetric_x('path/to/cell.bedpe.gz',
                               bin_data   = bins@data,
                               max_dist   = 100000)
sbird_sce <- process.batch(bams, bedpes, genome = 'hg38',
                           background_method = 'symmetric',
                           symmetric_x       = opt_x)
}
}
